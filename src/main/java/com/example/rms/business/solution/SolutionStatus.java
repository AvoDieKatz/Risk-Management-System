package com.example.rms.business.solution;

public enum SolutionStatus {
    PROPOSED, // A solution is proposed and waiting for
    ACCEPTED, // A solution is accepted as the approach to resolve the identified risk
    REJECTED, // A solution is rejected as

}
